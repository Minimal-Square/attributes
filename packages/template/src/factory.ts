// import { getAttribute } from './utils/selectors';

export const initSolutionElement = (solutionElement: HTMLElement) => {
  // Get the date on the element

  solutionElement.innerHTML = 'Hey!';
};
